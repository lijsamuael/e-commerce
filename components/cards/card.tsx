export default function Card({name, image, tag, price, color, rate, icon}:{name: string, image: string, tag?: string, price: number, color: string, rate?: number, icon?: string}){
    return(
        <div>Card is here</div>
    );
}